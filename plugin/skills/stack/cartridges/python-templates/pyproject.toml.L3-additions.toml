# Additions for L3 — merge into pyproject.toml
#
# specdrive is NOT a Python dependency. It's a language-agnostic CLI distributed
# on npm that audits the Gherkin specs + JUnit XML (not Python source), so it's
# run via `npx specdrive audit` from the justfile — see the python.md cartridge.
# Only the Python-native L3 tool (mutmut) belongs here.
[project.optional-dependencies]
dev = [
  # L0+L2 entries plus:
  "mutmut>=2.4",
]
